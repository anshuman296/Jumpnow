var primary = localStorage.getItem("primary") || '#ff0000';
var secondary = localStorage.getItem("secondary") || '#f5ea00';

window.CubaAdminConfig = {
	// Theme Primary Color
	primary: primary,
	// theme secondary color
	secondary: secondary,
};
